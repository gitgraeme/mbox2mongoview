{
  "name": "mbox-viewer-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run --prefix server start\" \"npm run --prefix client dev\"",
    "install-all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
